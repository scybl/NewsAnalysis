# Changelog

## 1.4.0 - 2026-07-08

相对于 1.3，1.4 把项目从“能看到运维状态”的后台控制台，推进到更完整的个人金融数据中台：数据抽检、冷热备份、日 K 覆盖、账户归档、治理页面和部署同步保护都进入统一流程。

### 提升

- 新增服务器数据审计和随机抽检脚本，能汇总新闻、日 K、分时、开盘啦和 Mongo 集合占用，并输出可下载报告。
- 新增空闲数据随机抽检调度器和前端展示页，把运维状态、数据抽检、审计日志收口到系统治理视图。
- 股票日 K 增加覆盖记录和缺口追踪能力，能记录每只股票缺少哪些交易日，并为后续补齐提供依据。
- 分时冷数据归档升级为按股票/年份对象管理，支持月度完整性策略、独立 worker 上传、上传后本地清理和百度网盘冷备份读取。
- 部署同步脚本区分安全同步和强制同步：`tongbu` 会构建但延迟激活正在运行任务，`qiangzhitongbu` 用于确认可中断时强制重启。
- 后台页面合并账户/归档账号入口，新增系统治理导航，运维表格状态说明、日志列对齐、中文月日时间格式和百度网盘凭据展示。
- 市场纵览默认回退到最近可用交易日数据，开盘啦缺失指标增加 best-effort 抓取和本地计算兜底。
- README 和 GitHub 仓库描述改为个人金融数据中台定位，并补充技术栈说明。
- 测试体系扩展到 API smoke、账户归档通路、前端契约、日 K 覆盖、冷备份、部署脚本和轻量性能基线。

### 兼容与部署影响

- Tushare 继续保持封存状态，新抓取和资料包更新不应依赖 Tushare，默认优先 AkShare，东方财富/腾讯用于正确性验证、字段补齐和行情兜底。
- 分时冷备份上传建议使用独立 `minute-cold-worker` 容器，避免 Web 发布或重启打断长时间上传。
- 本次版本包含新脚本和新前端文件；部署前需要确认这些新增文件已进入 Git 跟踪，否则服务器同步会缺文件。

## 1.3.0 - 2026-07-03

相对于 1.2，1.3 把项目从“需要 SSH 进服务器翻日志”的运行方式，推进到前端可见、后端可汇总、重 IO 可互斥、冷数据可自检的运维基线。

### 提升

- 新增只读运维状态接口 `/api/admin/ops/status`，统一汇总后台任务、分时冷数据上传、调度器、NewsCrawler 状态和资源占用。
- 新增后台“运维状态”页面 `/admin-ops.html`，展示总体状态、重 IO 占用、任务进度、数据覆盖、资源摘要和最近错误。
- 分时冷数据上传日志进入统一快照，能解析进度、当前股票/年份、最后事件和疑似卡住状态。
- 重 IO 任务入口接入互斥护栏，分时冷数据上传、全市场日 K、空闲分钟预抓等任务不会互相挤占服务器 IO。
- Guardian 冷索引支持去重预检，避免冷库已有文章被再次抓取或重复写入。
- 文档入口收口：移除未发布的历史参考笔记，修复 README 指向忽略文件的断链，并把 NewsCrawler/NewsAnalysis 边界说明移入正式 docs。
- 清理静态检查命中的无效代码：未使用导入、未使用变量、重复方法定义和不可达演示代码。

### 兼容与部署影响

- 1.3 不默认启动新的重任务，也不改变 NewsAnalysis 只读消费 NewsCrawler `news.v1` 文档的边界。
- Agent Gateway 和数据分发仍保持暂停/维护状态；相关页面只保留状态说明，不签发 token，不开放机器调用。
- 本次发布只更新本地代码与版本记录；生产服务器仍需按现有部署流程同步后才会运行 1.3。
